<% provide(:title, 'All Users') %>
<h1>CoffeeShop Users</h1>


<ul class="users">
	<% @users.each do |user| %>
		<li>
			<%= image_tag user.avatar.url(:small) %>
			<span> <%= link_to user.name, user %> </span> 
		</li>
	<% end %>
</ul>
